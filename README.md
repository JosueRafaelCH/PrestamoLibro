# PrestamoLibro
Sistema web con Spring Boot 3 + Thymeleaf + Spring Security + JPA/MySQL para gestionar préstamos de libros. CRUD de usuarios y préstamos, autocompletado AJAX, autenticación por roles, paginación y Bootstrap 5.
